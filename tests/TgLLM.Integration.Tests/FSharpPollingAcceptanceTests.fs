/// T028: F# façade acceptance over long polling — filled in once T027 (the façade) exists.
module TgLLM.Integration.Tests.FSharpPollingAcceptanceTests

open Expecto

[<Tests>]
let fSharpPollingAcceptanceTests = testList "FSharpPollingAcceptance" []
