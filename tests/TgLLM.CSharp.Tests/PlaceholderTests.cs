using Xunit;

namespace TgLLM.CSharp.Tests;

/// <summary>
/// Skeleton placeholder so the test project builds and `dotnet test` has something to discover.
/// The real C# façade behavior tests (and the FSharp-idiom-leak canary) land in Phase 3
/// (User Story 1, task T033), once <c>TgLLM.CSharp</c> ships a real public surface.
/// </summary>
public class PlaceholderTests
{
    [Fact]
    public void Placeholder_project_builds()
    {
        Assert.True(true);
    }
}
