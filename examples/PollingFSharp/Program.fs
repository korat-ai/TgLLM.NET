/// Skeleton example app. Rewritten to a runnable long-polling bot demo in Phase 3
/// (User Story 1, task T027) and extended with a proactive-send timer in Phase 4 (US2, T037).
module PollingFSharp.Program

[<EntryPoint>]
let main _ =
    printfn "TgLLM.NET PollingFSharp example — placeholder, implemented in a later phase."
    0
