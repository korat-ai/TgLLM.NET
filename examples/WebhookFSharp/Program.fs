/// Skeleton example app. Rewritten to a runnable webhook bot demo in Phase 3
/// (User Story 1, task T031) and used for the routing-at-scale example in Phase 5 (US3, T042).
module WebhookFSharp.Program

[<EntryPoint>]
let main _ =
    printfn "TgLLM.NET WebhookFSharp example — placeholder, implemented in a later phase."
    0
