/// Skeleton (T001, Setup): the full Tool Router demo — registering tools and building a
/// data-driven plan as a stand-in for an LLM decision, over both long polling and webhooks
/// (Principle VIII) — lands in T031 (Polish), out of scope for T001-T020. This skeleton only
/// proves the example project builds and references the F# façade project; it is replaced with a
/// real demo once the Tool Router façade surface (T017) exists.
module ToolRouterFSharp.Program

[<EntryPoint>]
let main _ =
    printfn "ToolRouterFSharp skeleton: the full Tool Router demo lands in T031 (Polish)."
    0
